apiVersion: v1
kind: Service
metadata:
  name: {{ include "joseph-test.fullname" . }}
  labels:
    {{- include "joseph-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "joseph-test.selectorLabels" . | nindent 4 }}
